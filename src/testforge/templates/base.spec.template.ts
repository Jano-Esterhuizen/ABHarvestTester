import { test, expect } from "@playwright/test";

test.describe("Feature: ${FEATURE_NAME}", () => {
  test("${ROLE} can ${ACTION}", async ({ page }) => {
    // Arrange
    // const model = new FeatureModel(page);

    // Act
    // await model.doAction();

    // Assert
    // await model.verifyOutcome();
  });
});
